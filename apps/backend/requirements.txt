# RNF-62 — versoes com correcao de seguranca (pip-audit): fastapi/starlette
# (starlette 1.6.0 zera os CVEs PYSEC-2026-161/248/249/1941/1942/2280/2281),
# click 8.3.3 (PYSEC-2026-2132), python-dotenv 1.2.3 (PYSEC-2026-2270),
# Mako 1.3.12 (PYSEC-2026-2617). Arquivo convertido de UTF-16 p/ UTF-8
# (o UTF-16 quebrava `pip-audit -r`). Ver README secao Seguranca.
aiosqlite==0.21.0
alembic==1.18.4
annotated-types==0.7.0
asyncpg==0.30.0
certifi==2026.2.25
click==8.3.3
colorama==0.4.6
Deprecated==1.3.1
dnspython==2.8.0
email-validator==2.3.0
fastapi==0.141.1
greenlet==3.4.0
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
iniconfig==2.3.0
joblib==1.5.3
limits==3.14.0
Mako==1.3.12
MarkupSafe==3.0.3
numpy==2.4.4
packaging==24.2
pandas==3.0.2
pluggy==1.6.0
pydantic==2.12.5
pydantic-settings==2.9.1
pydantic_core==2.41.5
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
PyYAML==6.0.3
scikit-learn==1.8.0
scipy==1.17.1
six==1.17.0
slowapi==0.1.9
sniffio==1.3.1
SQLAlchemy==2.0.40
starlette==1.6.0
structlog==25.3.0
threadpoolctl==3.6.0
typing-inspection==0.4.2
tzdata==2026.1
uvicorn==0.34.0
watchfiles==1.1.1
websockets==16.0
wrapt==2.1.2
xgboost==3.2.0
onnxruntime>=1.18.0
pandas>=2.1.0
pyarrow>=14.0.0
# RF-22 / RNF-46 -- cliente MCP (src/services/mcp_client.py). httpx ja
# presente acima para o cliente Ollama (src/services/ollama_client.py).
mcp==2.2.0

# RNF-50 / RNF-51 -- fila assincrona de notificacoes (celery-worker,
# src/tasks/notification_tasks.py). Broker Redis (CELERY_BROKER_URL) --
# sem result backend (fire-and-forget, task_ignore_result=True).
celery==5.4.0
redis==5.0.8

# RNF-76 / RNF-77 -- observabilidade de producao (GET /metrics, ver
# src/core/metrics.py). prometheus-client ja vinha transitivo (nenhuma
# outra lib do projeto expunha essa dependencia diretamente) -- fixado
# aqui explicitamente, mesmo padrao "toda linha pinada" do resto do
# arquivo.
prometheus-client==0.26.0
prometheus-fastapi-instrumentator==8.1.0

# RF-27 / RNF-55 -- monitoramento de data drift (Celery Beat,
# src/services/drift_monitor.py, src/tasks/drift_tasks.py). Ate a task de
# hardening de seguranca (RNF-62) usava `evidently` (Report/Dataset/
# DataDefinition/ValueDrift) para o calculo de PSI -- removido porque
# `evidently` carrega `nltk` como dependencia obrigatoria e imediata (o
# proprio `evidently/__init__.py` importa um modulo que faz `from nltk
# import ...`), e `nltk` tem uma vulnerabilidade High sem correcao
# publicada em nenhuma versao (PYSEC-2026-3740/CVE-2026-81726). O calculo
# de PSI foi reimplementado com numpy/pandas (ja dependencias abaixo/deste
# arquivo, nenhuma nova) em `drift_monitor.py::_population_stability_index`
# -- replica fiel do algoritmo do evidently, validada bit-a-bit contra ele.
# Ver docstring de drift_monitor.py e PENDENCIAS.md para o historico
# completo.
