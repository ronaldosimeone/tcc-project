# Gerado por package manager — nunca reformatar (estrutura exata é contrato
# com `pnpm install --frozen-lockfile`, usado no CI).
pnpm-lock.yaml

# dependências / build (mesma lista de .gitignore)
node_modules
.next
out
build
coverage
storybook-static
playwright-report
test-results
e2e-results

# gerado automaticamente — não é código-fonte
public/mockServiceWorker.js
*.tsbuildinfo
next-env.d.ts
