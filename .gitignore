node_modules/
.env
__pycache__/
apps/frontend/.pnpm-store/

# ML data – never commit datasets
apps/ml/data/
*.parquet
*.csv
apps/ml/checkpoints/

# Python
# Python Virtual Environment
.venv/
*.pyc
*.pyo
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Jupyter checkpoints
.ipynb_checkpoints/

# Playwright
playwright-report/
test-results/
e2e-results/
apps/frontend/public/mockServiceWorker.js

# Load test raw output (RNF-37/RNF-38) — reproduzível via locust_streaming.py
# / check_memory_leak.py; resumo dos resultados fica no README.
loadtest_results/

# Frontend perf/Lighthouse raw output (RNF-39/RNF-40) — reproduzível via
# apps/frontend/scripts/measure-perf.mjs + lighthouse; resumo fica em
# frontend_performance_report.md.
/frontend_perf_*.json
/lighthouse_*.json

# Ingestão de manuais (RF-20/RNF-44) — PDFs reais e o ChromaDB gerado nunca
# vão pro repo (dados grandes/binários, reproduzíveis via index_manuals.py).
apps/mcp-server/data/

# Celery Beat (RF-27/RNF-55) — arquivo de estado local do scheduler
# (shelve DB), criado em runtime dentro do container `celery-beat`;
# nunca deve ir pro repo (estado de máquina, não código).
apps/backend/celerybeat-schedule*